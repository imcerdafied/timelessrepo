{
  "name": "timeless",
  "scripts": {
    "build": "cd apps/frontend && npm install --legacy-peer-deps && npm run build && cd ../backend && npm install",
    "start": "cd apps/backend && node server.js"
  }
}
