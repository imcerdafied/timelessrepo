// BLE beacon detection would replace GPS in production.
// For the demo, BLE simulation is handled in the Zustand store
// and displayed in LocationSelector (ZoneSelector).
export default function GPSTrigger() {
  return null
}
export function useGPS() {
  return null
}
